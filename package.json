{
  "name": "binance-martingale-bot",
  "version": "1.0.0",
  "description": "Automated martingale trading bot for Binance Futures.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start:testnet": "BINANCE_NETWORK=testnet node --env-file=.env.testnet app.js",
    "start:mainnet": "BINANCE_NETWORK=mainnet node --env-file=.env.mainnet app.js",
    "test": "node --test"
  }
}
